cd "OneDrive - Pro QC International\Claude Code\FirstAppWithClaudeCode"
cd backend
.\venv\Scripts\Activate.ps1
# $env:ANTHROPIC_API_KEY = "sk-ant...."   # needed for AI matching; never commit the real key
python -m uvicorn app.main:app --reload --port 8000

Frontend (in a secdcond terminal):
cd frontend
npm run dev


http://localhost:5173/